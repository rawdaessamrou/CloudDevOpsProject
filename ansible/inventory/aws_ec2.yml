plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  tag:Role: jenkins
keyed_groups:
  - key: tags.Role
    prefix: role
hostnames:
  - public-ip-address
compose:
  ansible_host: public_ip_address